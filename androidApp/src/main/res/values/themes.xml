<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        App theme. The Compose `SplashScreen` is the only splash; we just need
        the pre-Compose window to render white so the brief frame before Compose
        mounts blends into the splash background (matches MyKittaColors.Background).
    -->
    <style name="Theme.MyKitta" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/ic_launcher_background</item>
    </style>
</resources>